# deps / build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — never commit any .env.* except the example
.env
.env.*
!.env.example

# misc
.DS_Store
.vercel
.claude/

# vendored supplier source data (on-disk only, not repo content — see vendor/images-source/
vendor/images-alt/README.md)
vendor/images-source/
vendor/images-alt/
