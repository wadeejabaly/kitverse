# deps / build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env — never commit any .env.* except the example
.env
.env.*
!.env.example

# misc
.DS_Store
.vercel
.claude/

# vendored image archives (on-disk only; data/scripts/payloads ARE tracked)
vendor/images-source/
vendor/images-alt/
